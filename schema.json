{
    "title": "Event",
    "description": "A smart door event",
    "type": "object",
    "properties": {
        "event": {
            "description": "The type of event that occurred",
            "type": "string"
        },
		"time": {
            "description": "The time at which the event occurred",
            "type": "string"
        },
        "pi": {
            "description": "The uuid of the smart door",
            "type": "string"
        },
        "user": {
			"description": "The uuid of the person associated with the event",
            "type": "string"
        }
    },
    "required": ["event", "time", "pi"]
}